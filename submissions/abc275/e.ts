import { readFileSync } from 'fs'
// wa
const main = (arg: string) => {
  const [N, M, K] = arg.trim().split(' ').map(x => +x)

  const MOD = 998244353n

  const rondom = Math.floor(Math.random() * (M + 1))


}



main(readFileSync('/dev/stdin', 'utf-8'))